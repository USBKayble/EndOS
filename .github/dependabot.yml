version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
# Note: removed npm updates at the repository root because this repo has no package.json at /
# Add additional package-ecosystem entries for subdirectories that contain package.json files, e.g.:
#  - package-ecosystem: "npm"
#    directory: "/some/subdir"
#    schedule:
#      interval: "weekly"
